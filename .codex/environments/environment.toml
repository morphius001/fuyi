# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "fuyi"

[setup]
script = '''
set -e

cd "${CODEX_WORKTREE_PATH:-$PWD}"

export NVM_DIR="$HOME/.nvm"
if [ -s "$NVM_DIR/nvm.sh" ]; then
  . "$NVM_DIR/nvm.sh"
  nvm install 24
  nvm use 24
fi

export BUN_INSTALL="$HOME/.bun"
export PATH="$BUN_INSTALL/bin:$PATH"

if ! command -v bun >/dev/null 2>&1; then
  echo "Bun is not installed or not in PATH."
  echo "Please install Bun in MyCustomWSL first:"
  echo "curl -fsSL https://bun.sh/install | bash"
  exit 1
fi

node -v
bun --version
bun install
'''

[cleanup]
script = '''
set -e

cd "${CODEX_WORKTREE_PATH:-$PWD}"

rm -rf .turbo
rm -rf .cache/tmp
rm -rf tmp
'''

[[actions]]
name = "Install"
icon = "tool"
command = '''
cd "${CODEX_WORKTREE_PATH:-$PWD}"

export NVM_DIR="$HOME/.nvm"
if [ -s "$NVM_DIR/nvm.sh" ]; then
  . "$NVM_DIR/nvm.sh"
  nvm use
fi

export BUN_INSTALL="$HOME/.bun"
export PATH="$BUN_INSTALL/bin:$PATH"

bun install
'''

[[actions]]
name = "Lint"
icon = "tool"
command = '''
cd "${CODEX_WORKTREE_PATH:-$PWD}"

export NVM_DIR="$HOME/.nvm"
if [ -s "$NVM_DIR/nvm.sh" ]; then
  . "$NVM_DIR/nvm.sh"
  nvm use
fi

export BUN_INSTALL="$HOME/.bun"
export PATH="$BUN_INSTALL/bin:$PATH"

bun run lint
'''

[[actions]]
name = "Check Types"
icon = "tool"
command = '''
cd "${CODEX_WORKTREE_PATH:-$PWD}"

export NVM_DIR="$HOME/.nvm"
if [ -s "$NVM_DIR/nvm.sh" ]; then
  . "$NVM_DIR/nvm.sh"
  nvm use
fi

export BUN_INSTALL="$HOME/.bun"
export PATH="$BUN_INSTALL/bin:$PATH"

bun run check-types
'''

[[actions]]
name = "Git Status"
icon = "tool"
command = '''
cd "${CODEX_WORKTREE_PATH:-$PWD}"
git status --short --branch
'''
